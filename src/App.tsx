import React from 'react';
import { hot } from 'react-hot-loader/root';
import './App.scss';

const App: React.FunctionComponent = () => {
    return <><span className="font">TypeScript Base with React, Webpack, Jest and Live Reloading 🙌 and other stuff!</span></>;
};

export default hot(App);