import React from 'react';
import { hot } from 'react-hot-loader/root';

const App: React.FunctionComponent = () => {
    return <><span>TypeScript Base with React, Webpack, Jest and Live Reloading 🙌</span></>;
};

export default hot(App);